
<%= form_for(@task, html: {class: "form-horizontal"}) do |f| %>
  <% if @task.errors.any? %>
  <div class="row">
    <div id="error_explanation" class="alert alert-danger col-md-8">
      <h2><%= pluralize(@task.errors.count, "error") %>
        prohibited this task from being saved:</h2>
      <ul>
      <% @task.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  </div>
  <% end %>

<div class="form-group">
  <%= f.label :description, class: "col-sm-1 control-label" %>
    <div class="col-sm-3">
  <%= f.text_field :description, class: "form-control" %>
  </div>
</div>

<div class="form-group">
    <%= f.label :due_date, class: "col-sm-1 control-label" %>
    <div class="col-sm-2">
      <%= f.date_field :due_date, class: "form-control" %>
    </div>
  </div>

<% if @task.persisted? %>
  <div class="form-group">
    <div class="col-sm-offset-1 col-sm-2">
      <%= f.check_box :completed %>
      Completed
    </div>
  </div>
<% end %>

<div class="form-group">
  <div class="col-sm-4 col-sm-offset-1">
    <div class="actions">
      <%= f.submit class: "btn btn-primary" %>
      <%= link_to 'Cancel', tasks_path, class: "btn btn-default btn-md" %>

  </div>
</div>

<% end %>
